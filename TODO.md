# TemperPass - Feature Backlog

_Flat list for now. Reorganize by brand pillars in Phase 6._

## Spine (this session)

- [x] Lock Phase 1 brief from spec v4
- [x] Lay out `passes/` (red-team locked, clarify-first drafted, two spines)
- [x] FilePress site builds and hero flow works locally
- [x] README and site carry spec copy (mill, four passes, install)

## Pass refinement (next)

- [x] Origin story on README, home, spec §13, and `/posts/a-prompt-with-coffee` (Vox 14 Aug 2026)
- [x] Lock `scope-lock` to `red-team` depth; record failed runs in `examples/`
- [x] Lock `tradeoff-matrix` the same way
- [x] Validate `clarify-first` — negative cases first (false-trigger rate)
- [ ] Independent runs of all four passes (current transcripts are same-session)
- [ ] Run `red-team` on a non-numeric decision (pricing, hiring, go-to-market)

## Publish

- [ ] Make `Catalyst-Forge-LLC/temper-pass` public
- [ ] Confirm domain temperpass.dev and Cloudflare Pages project `temperpass`
- [ ] `pnpm ship` and attach the custom domain
- [ ] Shelf entry on catalystforge.com/open-source/
- [ ] Publish npm `temperpass` only when the package shape is ready
- [ ] `temper` CLI (verb-first) — later

## Parked (README roadmap only — no empty skill dirs)

- [ ] `first-principles`, `option-generator`, `premortem`, `steelman`, `confidence-calibrate` — each returns only with a protocol that beats "just ask"
