{
	"name": "temperpass-site",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "filepress dev",
		"build": "filepress build",
		"preview": "filepress preview",
		"check": "filepress check",
		"ship": "pnpm build && wrangler pages deploy build --project-name=temperpass",
		"deploy": "pnpm ship"
	},
	"devDependencies": {
		"getfilepress": "^0.1.3",
		"wrangler": "^4.28.0"
	}
}
